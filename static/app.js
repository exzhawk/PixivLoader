// Generated by CoffeeScript 1.10.0
(function() {
  $(function() {
    var $$big, $big, $thumbnail, change_to, current_page, encode_css_background_image_url, init, load_page, loading, set_big_background_to_page;
    init = true;
    loading = false;
    $big = $('#big');
    $$big = $big[0];
    $thumbnail = $('#thumbnail');
    $thumbnail.owlCarousel({
      center: true,
      nav: false,
      dots: false,
      responsive: {
        0: {
          items: 5
        },
        1200: {
          items: 9
        },
        1800: {
          items: 13
        }
      }
    }).on('mousewheel', function(e) {
      if (e.deltaY < 0) {
        $thumbnail.trigger('next.owl');
      } else {
        $thumbnail.trigger('prev.owl');
      }
      return e.preventDefault();
    }).on('click', '.owl-item', function(e) {
      e.preventDefault();
      return $thumbnail.trigger('to.owl.carousel', [$(this).index(), 200, true]);
    }).on('changed.owl.carousel', function(e) {
      var count, current;
      current = e.item.index;
      change_to(current);
      count = e.item.count;
      if (count - current < 20) {
        if (loading === false) {
          loading = true;
          return load_page(current_page);
        }
      }
    });
    $('#open_touch').on('mousewheel', function(e) {
      if (e.deltaY < 0) {
        $$big.next_pic();
      } else {
        $$big.prev_pic();
      }
      return e.preventDefault();
    });
    $$big.next_pic = function() {
      if ($$big.img_data['is_manga']) {
        if ($$big.current_page < $$big.total_page) {
          $$big.current_page += 1;
          return set_big_background_to_page($$big.current_page);
        }
      }
    };
    $$big.prev_pic = function() {
      if ($$big.img_data['is_manga']) {
        if ($$big.current_page > 0) {
          $$big.current_page -= 1;
          return set_big_background_to_page($$big.current_page);
        }
      }
    };
    change_to = function(index) {
      var img_data;
      img_data = $thumbnail.find('.owl-stage').children('.owl-item').eq(index).find('.thumbnail_img')[0].img_data;
      $$big.img_data = img_data;
      $('#caption').text(img_data['caption']);
      $('#title').text(img_data['title']).attr('href', 'http://www.pixiv.net/member_illust.php?mode=medium&illust_id=' + img_data['id']);
      $('#author').text(img_data['user']['name']).attr('href', 'http://www.pixiv.net/member_illust.php?id=' + img_data['user']['id']);
      if (img_data['is_manga']) {
        $$big.pages = img_data['metadata']['pages'];
        $$big.total_page = $$big.pages.length - 1;
        $$big.current_page = 0;
        return set_big_background_to_page($$big.current_page);
      } else if (img_data['type'] === 'ugoira') {
        $$big.zip_urls = img_data['metadata']['zip_urls']['ugoira600x600'];
        return $$big.frames = img_data['metadata']['frames'];
      } else {
        return $big.css('background-image', encode_css_background_image_url(img_data['image_urls']['large']));
      }
    };
    set_big_background_to_page = function(page_number) {
      return $big.css('background-image', encode_css_background_image_url($$big.pages[page_number]['image_urls']['large']));
    };
    encode_css_background_image_url = function(url) {
      return 'url("/get_file/' + encodeURIComponent(url) + '")';
    };
    $('#prev_touch').on("click", function(e) {
      $thumbnail.trigger("prev.owl");
      return e.preventDefault();
    });
    $('#next_touch').on("click", function(e) {
      $thumbnail.trigger("next.owl");
      return e.preventDefault();
    });
    load_page = function(page_number) {
      $.getJSON({
        url: '/get_following/' + page_number,
        success: function(data) {
          var $img, $item, i, illust, len;
          for (i = 0, len = data.length; i < len; i++) {
            illust = data[i];
            $img = $('<div>').addClass('thumbnail_img').css('background-image', encode_css_background_image_url(illust['image_urls']['small']));
            $img[0].img_data = illust;
            if (illust['is_manga']) {
              $img.addClass('multi');
            } else if (illust['type'] === 'ugoira') {
              $img.addClass('ugoira');
            } else {
              $img.addClass('illust');
            }
            $item = $('<div>').addClass('owl-item').append($img);
            $thumbnail.trigger("add.owl.carousel", [$item]);
          }
          $thumbnail.trigger("refresh.owl.carousel");
          if (init === true) {
            change_to(0);
            init = false;
          }
          return loading = false;
        }
      });
      return current_page += 1;
    };
    $(document).on('keydown', function(e) {
      var current_page, shown_count, total_count;
      switch (e.which) {
        case 33:
          shown_count = $('.owl-stage .active').length + 1;
          current_page = $('.owl-stage .center').index('.owl-stage > .owl-item');
          return $thumbnail.trigger('to.owl.carousel', [current_page - shown_count, 200, true]);
        case 34:
          shown_count = $('.owl-stage .active').length + 1;
          current_page = $('.owl-stage .center').index('.owl-stage > .owl-item');
          return $thumbnail.trigger('to.owl.carousel', [current_page + shown_count, 200, true]);
        case 35:
          total_count = $('.owl-stage > .owl-item').length;
          return $thumbnail.trigger('to.owl.carousel', [total_count - 1, 200, true]);
        case 36:
          return $thumbnail.trigger('to.owl.carousel', [0, 200, true]);
        case 37:
          return $thumbnail.trigger('prev.owl');
        case 38:
          return $$big.prev_pic();
        case 39:
          return $thumbnail.trigger('next.owl');
        case 40:
          return $$big.next_pic();
      }
    });
    current_page = 1;
    return load_page(current_page);
  });

}).call(this);

//# sourceMappingURL=app.js.map
